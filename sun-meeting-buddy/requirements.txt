# Sunny Bad Buddy Timer runs on the Python standard library (Tkinter) alone.
#
# Pillow is recommended: it enables the spin & squish animation. Without it,
# Sunny still flies in, roams, bounces, and plays sounds -- he just won't
# spin or squish.
#
#   pip install pillow
#
Pillow>=9.0
