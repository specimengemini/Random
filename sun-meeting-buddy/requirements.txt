# Sunny Bad Buddy Timer runs on the Python standard library (Tkinter) alone,
# but two optional packages unlock extras:
#
#   Pillow   -> the spin & squish animation (without it he still roams/bounces)
#   pystray  -> the system-tray icon (without it the tray icon is skipped)
#
#   pip install pillow pystray
#
Pillow>=9.0
pystray>=0.19
